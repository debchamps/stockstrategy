function updateOrderUpdate(orderUpdate) {}

function bracketCNCOrderUpdate(orderUpdate) {
  //If it was a stopLoss sell order.
  //If it was a limit sell order.
  //Cancel the other order.
  //Place another CNC bracket order. with stopLoss = price - 5% trigger = price + 3%
}
